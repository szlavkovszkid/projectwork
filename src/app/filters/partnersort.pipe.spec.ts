import { PartnersortPipe } from './partnersort.pipe';

describe('PartnersortPipe', () => {
  it('create an instance', () => {
    const pipe = new PartnersortPipe();
    expect(pipe).toBeTruthy();
  });
});
